{
  "name": "dao-proposal-system",
  "version": "1.0.0",
  "description": "On-chain proposal system with quorum rules and execution timelock",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0"
  },
  "author": "rukinem",
  "license": "MIT"
}